# services:
#   db:
#     image: pgvector/pgvector:pg15
#     container_name: epadm_db
#     ports:
#       - "5433:5432"
#     environment:
#       # The `postgres` superuser is used ONLY for migrations/bootstrap (DDL).
#       # Application traffic must connect as the non-superuser `epadm_app` role so
#       # that FORCE ROW LEVEL SECURITY is enforced. See docker/init/01-roles.sql
#       # and docs/DATABASE_ROLES.md.
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: epadm
#       POSTGRES_HOST_AUTH_METHOD: scram-sha-256
#       POSTGRES_INITDB_ARGS: "--auth-host=scram-sha-256"
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#       # Runs once on a fresh volume to create the epadm_app / epadm_ops roles.
#       - ./docker/init:/docker-entrypoint-initdb.d:ro
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U postgres -d epadm"]
#       interval: 5s
#       timeout: 5s
#       retries: 5

# volumes:
#   postgres_data:




services:
  redis:
    image: redis:7-alpine
    container_name: redis-service
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    restart: always

volumes:
  redis_data: