import { DELETE as primaryDelete } from "../../v1/hr/departments/[id]/route"; import { deprecated } from "@/lib/phase3/api"; type Context={params:Promise<{id:string}>};export async function DELETE(request:Request,context:Context){const{id}=await context.params;return deprecated(await primaryDelete(request,{params:Promise.resolve({id})}),`/api/v1/hr/departments/${id}`)}
