import { GET as primaryGet, POST as primaryPost } from "../../v1/administration/memberships/route"; import { deprecated } from "@/lib/phase3/api"; export async function GET(request:Request){return deprecated(await primaryGet(request),"/api/v1/administration/memberships")}export async function POST(request:Request){return deprecated(await primaryPost(request),"/api/v1/administration/memberships")}
