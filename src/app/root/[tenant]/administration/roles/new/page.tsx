import { PageHeader } from "@/components/layout/page-header";
import { Card } from "@/components/ui/card";
import { requirePermission } from "@/lib/auth/guards";
import { PERMISSION_DESCRIPTIONS, USER_ROLES } from "@/lib/auth/catalog";
export default async function NewRolePage() { await requirePermission("administration.roles.create"); return <div className="space-y-6"><PageHeader title="Create custom role" description="Choose a base role and explicit permission overrides."/><Card padding="lg"><form className="space-y-5" data-api="/api/v1/administration/roles"><label className="grid gap-1 text-sm">Name<input name="name" required className="rounded-md border px-3 py-2"/></label><label className="grid gap-1 text-sm">Description<textarea name="description" className="rounded-md border px-3 py-2"/></label><label className="grid gap-1 text-sm">Base role<select name="baseRole" className="rounded-md border px-3 py-2">{USER_ROLES.map((role)=><option key={role}>{role}</option>)}</select></label><div><h2 className="font-medium">Permission matrix</h2><div className="mt-3 grid max-h-96 gap-2 overflow-auto md:grid-cols-2">{Object.entries(PERMISSION_DESCRIPTIONS).map(([permission, description])=><label key={permission} className="flex gap-2 rounded border p-3 text-sm"><input type="checkbox" name="permissions" value={permission}/><span><strong>{permission}</strong><span className="block text-xs text-muted-foreground">{description}</span></span></label>)}</div></div><button className="rounded-md bg-primary px-4 py-2 text-sm text-primary-foreground">Create role</button></form></Card></div>; }
