import { PageHeader } from "@/components/layout/page-header"; import { Badge } from "@/components/ui/badge"; import { requirePermission } from "@/lib/auth/guards"; import { listStaffDepartments } from "@/lib/admin/registries";
export default async function Page(){const ctx=await requirePermission("hr.departments.read");const rows=await listStaffDepartments(ctx.tenantId);return <div className="space-y-6"><PageHeader title="Departments" description="Department ownership, staffing, and vacancies."/><div className="overflow-x-auto rounded-lg border"><table className="w-full text-sm"><thead className="bg-muted/50 text-left"><tr>{["Department","Head","Staff","Vacancies","Status","Actions"].map(h=><th key={h} className="px-4 py-3">{h}</th>)}</tr></thead><tbody>{rows.map(d=><tr key={d.id} className="border-t"><td className="px-4 py-3"><p className="font-medium">{d.name}</p><p className="text-xs text-muted-foreground">{d.code}</p></td><td className="px-4 py-3">{d.headName??"—"}</td><td className="px-4 py-3">{d.activeCount} active / {d.headcount} total</td><td className="px-4 py-3">{d.vacancies}</td><td className="px-4 py-3"><Badge variant={d.status==="active"?"success":"default"}>{d.status}</Badge></td><td className="px-4 py-3 text-muted-foreground">{d.headcount?"Archive blocked":"Edit · Archive"}</td></tr>)}</tbody></table></div></div>}
