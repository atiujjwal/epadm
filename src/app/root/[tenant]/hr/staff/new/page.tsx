import { PageHeader } from "@/components/layout/page-header"; import { requirePermission } from "@/lib/auth/guards"; import { listStaffDepartments } from "@/lib/admin/registries"; import { StaffCreateForm } from "./staff-create-form"; export default async function Page(){const ctx=await requirePermission("hr.staff.create");return <div className="space-y-6"><PageHeader title="Add staff member" description="Create personal, employment, and contact information."/><StaffCreateForm departments={(await listStaffDepartments(ctx.tenantId)).map(d=>d.name)}/></div>}
