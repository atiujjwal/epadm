import { PageHeader } from "@/components/layout/page-header"; import { ImportWorkflow } from "@/components/import-workflow"; import { requirePermission } from "@/lib/auth/guards"; import { listImportJobs } from "@/lib/phase3/imports"; export default async function Page(){const ctx=await requirePermission("hr.staff.import");return <div className="space-y-6"><PageHeader title="Import staff" description="Validated, idempotent CSV imports with row-level errors."/><ImportWorkflow entityType="staff" jobs={await listImportJobs(ctx.tenantId,"staff")}/></div>}
